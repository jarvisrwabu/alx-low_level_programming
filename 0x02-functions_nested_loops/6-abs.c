#include "main.h"
#include <stdlib.h>
/**
 * _abs - returns absolute of a number
 * @n: The number to get the absolute of
 *
 * Return: The absolute of the number
 *
 */

int _abs(int n)
{
	return (abs(n));
}
