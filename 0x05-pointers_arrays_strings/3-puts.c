#include "main.h"
#include <string.h>

/**
 * _puts - This function returns the length of a string
 *
 * @str: The pointer to the string
 *
 * Return: Nothing
 */

void _puts(char *str)
{
	puts(str);	/* puts() is a standard c function */
}
